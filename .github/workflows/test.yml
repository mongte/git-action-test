name: GitHub Actions TEST2
on:
  workflow_dispatch:
    inputs:
      deploy-target:
        description: 'des ??'
        required: true
        default:  'test1'
        type: choice
        options:
          - test1
          - test2
env:
  NODE_VERSION: 16.x
  DEPLOY_TARGET: ${{ github.event.inputs.deploy-target }}
  DEPLOY_BRANCH: ${{ github.ref_name }}
  FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
jobs:
  test1:
    name: deploy test1
    runs-on: ubuntu-latest
    steps:
      - run: echo " github.event_name -  ${{ github.event_name }}"
      - run: echo " runner.os - ${{ runner.os }}"
      - run: echo " github.ref - ${{ github.ref }}"
      - run: echo " github.repository - ${{ github.repository }}"
      - run: echo " github.ref_name - ${{ github.ref_name }}"
      - run: echo " github.event.inputs.deploy-target - ${{ github.event.inputs.deploy-target }}"
      - name: Check out repository code - actions/checkout@v3
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 #모든 태그 및 분기에 대한 모든 기록
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
  test2:
    name: deploy test2
    runs-on: ubuntu-latest
    steps:
      - run: echo "test2"

